# Host for the PartyKit realtime server used by Create Room / multiplayer.
# Local dev (run `npx partykit dev` in a separate terminal): 127.0.0.1:1999
# Production (after `npx partykit deploy`): <project>.<your-partykit-username>.partykit.dev
NEXT_PUBLIC_PARTYKIT_HOST=127.0.0.1:1999
